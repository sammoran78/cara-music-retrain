$schema: https://azuremlschemas.azureedge.net/latest/commandJob.schema.json
type: command
command: >-
  python ace_step_hybrid_stages.py
  --stage prepare_tensors
  --input_data ${{inputs.input_data}}
  --output_dir ${{outputs.output_dir}}
  --checkpoint ${{inputs.checkpoint}}
  --planner_checkpoint ${{inputs.planner_checkpoint}}
  --dit_variant ${{inputs.dit_variant}}
  --max_rows ${{inputs.max_rows}}
  --dashboard_triggered ${{inputs.dashboard_triggered}}
  --dry_run ${{inputs.dry_run}}
code: ../../src
inputs:
  input_data:
    type: uri_folder
    mode: ro_mount
    path: azureml://datastores/ds_cara_raw_audio/paths/finetune-subset/
  checkpoint: ACE-Step/Ace-Step1.5
  planner_checkpoint: ACE-Step/acestep-5Hz-lm-0.6B
  dit_variant: base_or_sft_dit
  max_rows: 0
  dashboard_triggered: "false"
  dry_run: "false"
outputs:
  output_dir:
    type: uri_folder
    mode: rw_mount
    path: azureml://datastores/ds_cara_raw_audio/paths/training-runs/cara-strong-v0.4/ace_step/tensors/
environment: azureml:env-ace-step:5
compute: azureml:gpu-smoke-h100
identity:
  type: user_identity
experiment_name: cara-ace-step-smoke
display_name: 19-prepare-ace-step-dataset-json-tensors
description: Build CARA-labelled ACE-Step JSON-mode dataset, tensor manifest, registry resolver, and Side-Step command contract for the Hybrid branch.
tags:
  cara_phase: fine-tuning
  cara_step_id: "03"
  cara_step_name: 03_prepare_ace_step_dataset_json_tensors
  cara_compute_class: h100_preferred_else_cpu
  cara_model_family: ace_step
  cara_planner_size: 0.6B
  cara_training_gate: ace_step_tensor_prepare
  cara_training_scope: dataset-json-tensor-prep
  cara_cost_guardrail: existing_azureml_workspace_only
  cara_output_path: azureml://datastores/ds_cara_raw_audio/paths/training-runs/cara-strong-v0.4/ace_step/tensors/
