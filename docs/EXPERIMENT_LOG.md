# Experiment Log

- Initial scaffold implemented from `implement-plan.md`
- Subset-first execution strategy adopted for Phases 1-5
- Azure ML test-prep evidence is recorded in `docs/azureml_test_prep_evidence.md`.
- Fine-tuning gate order, smoke defaults, prepared dataset paths, and progress monitoring are recorded in `docs/cara_finetuning_runbook.md`.
- 2026-06-04: Stable Audio prepared dataset completed at 73,781 / 73,781 chunks. Submitted GPU-only Stable Audio smoke trainer job `witty_watch_v2dx7gd8vc` on `gpu-smoke-h100` with 250 steps, batch size 8, learning rate 1e-5, and checkpoint `stabilityai/stable-audio-open-small`.
- 2026-06-04: Re-checked the Stable Audio smoke trainer against `CARA-Strong_Plan_v0.4.docx`. The submitted job is classified as the `no_cara_baseline` trainer-plumbing smoke, not CARA-Strong evidence. The trainer now reports its variant and rejects `cara_strong` until structured conditioners, hidden-state taps, attribution head, non-detached auxiliary loss, calibration, and registry lookup are implemented.
- 2026-06-04: Downloaded failed job `witty_watch_v2dx7gd8vc` logs. Failure was environmental: CUDA/H100 was visible, but `pytorch_lightning` was missing from `env-stable-audio-tools:2`. Added `env-stable-audio-tools:3` with explicit Stable Audio training dependencies and pointed the smoke trainer job at v3.
- 2026-06-04: Downloaded failed job `brave_tomato_wn2lfp61vm` logs. Environment v3 reached H100/CUDA and prepared manifest loading (`73,781` rows; `61,325` train rows), then failed before training because `stabilityai/stable-audio-open-small` is gated on Hugging Face and the Azure job was not authenticated. Root exception: `GatedRepoError` downloading `model_config.json`.
- 2026-06-04: Hugging Face browser access for `stabilityai/stable-audio-open-small` was granted. Updated the smoke trainer flow to `env-stable-audio-tools:4`: the dashboard syncs local `.env` `HF_TOKEN` into the Azure ML workspace Key Vault secret `hf-token`, passes only Key Vault reference environment variables to the job, and the trainer retrieves the token at runtime before loading the gated checkpoint.
- 2026-06-04: Local Hugging Face gated config fetch succeeded for `model_config.json` using `.env` `HF_TOKEN` (`HTTP 200`, 5,622 bytes). Registered `env-stable-audio-tools:4` in Azure ML. Attempting to write workspace Key Vault secret `hf-token` failed with Key Vault `AccessDenied` because the current Azure CLI identity lacks secret set permission on vault `rgcaraaudiotra7394687711`; no smoke trainer job was launched.
- 2026-06-05: Downloaded failed v4 smoke job `clever_heart_hr46mgqy7q`. Key Vault auth succeeded (`hf_auth_source=azure_key_vault`), H100/CUDA was visible, the Stable Audio checkpoint loaded, and the prepared manifest/dataloader reported `73,781` rows, `61,325` train rows, and `256` batches over `2,048` linked smoke files. The remaining failure is a post-load `FileNotFoundError(2, 'No such file or directory')`; patched the smoke trainer to record `stage`, `limited_audio_file_count`, first-batch shape, and full traceback before any retry.
- 2026-06-05: Downloaded failed v4 smoke job `careful_papaya_dgz4zn09h4`. The diagnostic patch confirmed the first real dataloader batch loads (`[8, 2, 524288]`) and the failure occurs at `stage=create_training_wrapper`. Root cause: the gated Stable Audio Open Small `model_config.json` includes `training.arc.discriminator_base_ckpt=/path/to/base/rf/model`, a placeholder discriminator/RF checkpoint path. Patched the baseline smoke trainer to remove the `training.arc` block at runtime for `no_cara_baseline`/`cara_lite`, forcing the plain `DiffusionCondTrainingWrapper` path for trainer-plumbing smoke.
- 2026-06-05: Downloaded failed smoke job `willing_eagle_l86fk685m8`. The `training.arc` patch worked and the trainer reached `training_wrapper_mode=diffusion_cond_plain`, then failed importing Stable Audio Tools' diffusion wrapper because `auraloss` was missing. Added `auraloss==0.4.0` and bumped the smoke trainer to `env-stable-audio-tools:5`; preprocessing jobs remain on the existing v2 environment.
- 2026-06-05: Downloaded failed smoke job `silver_star_rt8db41589`. Environment v5 fixed `auraloss` and again reached `training_wrapper_mode=diffusion_cond_plain`, then failed importing Stable Audio Tools' `interface.aeiou` helper because `matplotlib` was missing. Added `matplotlib==3.8.4` and bumped the smoke trainer to `env-stable-audio-tools:6`; no new training job was launched by this fix.
- 2026-06-05: Downloaded failed smoke job `epic_snail_rgg9sffc6q`. Environment v6 fixed `matplotlib` and again reached `training_wrapper_mode=diffusion_cond_plain`, then failed importing Stable Audio Tools' semantic losses because `audiotools` was missing. Added `descript-audiotools==0.7.2` and bumped the smoke trainer to `env-stable-audio-tools:7`; no new training job was launched by this fix.
- 2026-06-05: To stop repeated full smoke failures during `create_training_wrapper`, aligned the smoke trainer environment with Stable Audio Tools' upstream training extras and bumped to `env-stable-audio-tools:8` (`descript-audio-codec`, `encodec`, `inf-cl`, `laion-clap`, `pytorch-lightning==2.5.5`, plus the previously discovered imports). Added a `07a` trainer preflight gate so the dashboard can validate deep training imports and wrapper construction before enabling the H100 smoke trainer.
- 2026-06-05: Stable Audio baseline smoke job `jovial_duck_0qs02zbv6t` completed successfully on `gpu-smoke-h100` with `env-stable-audio-tools:8`. Report status was `passed`, `global_step=250`, `batch_size=8`, `learning_rate=1e-5`, `variant=no_cara_baseline`, `limited_audio_file_count=2048`, and `training_seconds=288.036`. Checkpoints were written to the Azure datastore output path, including `epoch=0-step=250.ckpt` and `last.ckpt` at about 4.4 GB each; final metrics row reported `train/loss=0.8959378242492676`.
- 2026-06-05: Attempted to launch the CARA-lite Stable Audio smoke, but verification showed job `frank_mangos_7p5vmzrm57` had `cara_variant=cara_lite` in tags/output path while the actual Azure command input still serialized as `variant=no_cara_baseline`. Canceled the job before proceeding. Root cause was dashboard/backend mutation of Azure ML `NodeInput._data` without updating the command builder `job_inputs`; patched backend submissions to use `_azureml_set_job_input`, which updates the serialized job input and component default together.
- 2026-06-05: Updated the diffusion fine-tuning dashboard to keep the completed `no_cara_baseline` smoke as protocol step 05 and add the CARA-lite smoke as step 06 rather than rewriting the completed baseline step. The backend readiness payload now reports a Stable Audio smoke sequence with per-variant Azure job status, so canceled or failed attempts do not count as passed and the dashboard can show the next valid benchmark testing action.
- 2026-06-05: Job `mango_reggae_86qp6bwxh7` was observed after a restart/sign-in cycle. Azure tags and local registry labelled it `cara_lite`, but the actual Azure command inputs were still `variant=no_cara_baseline`, `dashboard_triggered=false`, and `run_name=cara-stable-audio-smoke`; it therefore does not count as a valid CARA-lite smoke. Added a typed launch-confirmation phrase to `/api/training/start`, removed the duplicate lower launch button, and added an immediate post-submit Azure input sanity check that requests cancellation if serialized command inputs do not match the dashboard request.
- 2026-06-05: The diffusion dashboard briefly displayed `06 CARA-Lite Smoke Passed` after `mango_reggae_86qp6bwxh7` because the smoke-sequence readiness check trusted registry event variant/tags. Patched readiness so a smoke step passes only when the Azure job is completed, uses the expected environment, and its serialized command inputs match the registry event (`variant`, `dashboard_triggered=true`, and `run_name`). Mismatched completed jobs now stay visible as non-passing evidence with the serialized input mismatch recorded in the readiness payload.
- 2026-06-05: Corrected the smoke-sequence pass rule for the historical baseline smoke. The verified `no_cara_baseline` job predates the typed launch-confirmation guard, so readiness now allows a completed baseline to pass when Azure serialized `variant=no_cara_baseline` on the expected environment, while retaining strict `dashboard_triggered=true` and `run_name` matching for CARA-lite and later stages.
- 2026-06-05: CARA-lite launch attempt `joyful_soccer_s2g7v63ldh` was rejected by the new post-submit sanity check because Azure again serialized default baseline command inputs (`variant=no_cara_baseline`, `dashboard_triggered=false`, `run_name=cara-stable-audio-smoke`) despite CARA-lite tags. Cancellation was requested and Azure reported `CancelRequested`. Root cause is Azure ML SDK object mutation not reliably overriding scalar command-job inputs loaded from the YAML. Patched the backend to materialize a per-run temporary Azure job YAML with requested scalar inputs before `load_job`; local serialization now shows `variant=cara_lite`, `dashboard_triggered=true`, and `run_name=cara-finetune-001-cara-lite` before submission.
- 2026-06-05: Implemented the next Stable Audio smoke ladder stages: `cara_head` and `cara_strong`. The smoke trainer now validates prepared CARA labels, writes a `cara_registry_resolver.json` artifact, attaches a hierarchical pool/family attribution head to Stable Audio hidden-state taps, and logs registry-decoded CARA-id predictions plus calibration/accuracy metrics. `cara_head` freezes/detaches the backbone for head-only evidence; `cara_strong` uses non-detached attribution loss with default weight `0.05` for CARA-Strong smoke evidence. The dashboard now keeps steps 05 and 06 intact and exposes ordered launch buttons for steps 07 and 08 with typed confirmations.
- 2026-06-05: CARA attribution-head smoke job `modest_rod_4v44xnpyc8` failed before training at Lightning optimizer setup with `ValueError('optimizer got an empty parameter list')`. The detached `cara_head` path froze the Stable Audio backbone but still called the Stable Audio Tools diffusion optimizer builder, leaving no trainable diffusion params before the head could be added. Patched `cara_head` to use a head-only AdamW optimizer, while `cara_strong` keeps the normal Stable Audio optimizer and adds the head params. Added a regression test for the frozen-head optimizer path.
- 2026-06-05: Step 07 CARA attribution-head smoke job `eager_lemon_rx67hzy41k` completed on `gpu-smoke-h100` with `env-stable-audio-tools:8`. Report status was `passed`, `variant=cara_head`, `global_step=250`, `limited_audio_file_count=2048`, `training_seconds=252.918`, `optimizer_mode=head_only`, `detach_features=true`, `freeze_backbone=true`, and registry-decoded CARA IDs were valid. Last logged smoke metrics included `cara/pool_top1=1.0`, `cara/pool_top5=1.0`, `cara/family_top1=1.0`, `cara/hierarchical_valid=1.0`, and `cara/ece=0.15344902873039246`. This validates label mapping, hidden-state taps, registry decoding, and head-only optimizer plumbing; it is not CARA-Strong evidence because the backbone was detached/frozen.
- 2026-06-05: Step 08 CARA-Strong smoke job `olive_pin_ms33zkw1l3` completed on `gpu-smoke-h100` with `env-stable-audio-tools:8`. Report status was `passed`, `variant=cara_strong`, `global_step=250`, `limited_audio_file_count=2048`, `training_seconds=322.582`, `optimizer_mode=stable_audio_plus_head`, `detach_features=false`, `freeze_backbone=false`, and registry-decoded CARA IDs were valid. Last logged smoke metrics included `cara/pool_top1=0.875`, `cara/pool_top5=1.0`, `cara/family_top1=1.0`, `cara/hierarchical_valid=1.0`, and `cara/ece=0.15926945209503174`. This proves the non-detached attribution path can run and backpropagate on H100, but the metrics are still limited smoke/train-batch evidence over 2,048 linked files.
- 2026-06-05: Implemented the step 09 full Stable Audio CARA-Strong fine-tune handoff learned from steps 07/08. Added `azureml/jobs/09_full_stable_audio_cara_strong_trainer.yml`, dashboard/backend support for `training_scope=full`, all-train-row launch defaults (`max_train_files=0`), held-out validation/test evaluation controls (`max_eval_files=0`, `max_eval_batches=0`), and a separate typed confirmation phrase. The full path remains GPU-only on existing Azure ML compute/datastore/environment resources and fails if validation/test CARA metrics are not produced.
- 2026-06-05: Tightened the full Stable Audio CARA-Strong trainer design after rechecking Stable Audio Tools conditioning docs/source. `cara_lite` remains the prompt-tag control, but `cara_strong` now keeps ordinary prompt text unchanged and injects `cara_pool_index` plus `cara_pool_family_index` as native Stable Audio `int` conditioners joined to DiT cross-attention conditioning. Prepared chunk generation now preserves `cara_pool_codeword` and `cara_registered_codeword`; older prepared manifests can derive the short codeword from the full `cara_pool_id` and report that derivation in `cara_codeword_manifest_summary`.
- 2026-06-05: Added the Stable Audio training-run progress tracker requested for long fine-tunes. `/api/training/readiness` now includes `stable_audio_training_progress`, `/api/training/run-progress` exposes the same read-only payload directly, and the diffusion fine-tuning page shows observed MLflow step, step percent, chunks-seen estimate, epoch estimate, latest loss, elapsed time, ETA, and Studio link. Chunk/epoch progress is calculated from `observed_step * batch_size` against the prepared train chunk count and is labelled as an estimate because shuffled training can revisit chunks.
- 2026-06-05: Added the step 09 `Full training run` dashboard checkbox. When checked, the max-steps input is disabled and the full Stable Audio launch sends `max_steps=0` plus `full_training_run=true`; the trainer resolves that sentinel to the prepared train dataloader length for a full dataset pass. Smoke launches still require an explicit bounded max-step value.
- 2026-06-06: Updated the diffusion fine-tuning ladder so step 04 Trainer Preflight and step 08 CARA-Strong Smoke are deliberately repeatable before step 09. This keeps previous passed runs as append-only evidence while allowing a clean post-change preflight/smoke revalidation path before the full CARA-Strong fine-tune.
- 2026-06-06: Step 08 CARA-Strong rerun `neat_nose_24xwtv0kfw` failed during `trainer_fit` with `KeyError('cara_pool_index')`. The report confirmed the first batch metadata contained `cara_pool_index` and `cara_pool_family_index`, but Stable Audio's loaded conditioner did not emit those tensors after the stricter native-conditioner config change. Patched the smoke trainer to add CARA `IntConditioner` modules to the already-loaded pretrained model conditioner before creating the Lightning wrapper, preserve existing prompt/time conditioners, and record a first-batch CARA conditioner preview so this failure mode is caught before the training loop.
- 2026-06-06: Fixed the diffusion dashboard reload state after a repeated step 04 preflight. When the original Stable Audio preprocessing submission falls out of the short recent-job list, downstream evidence such as passed preflight/smoke/full-training jobs now proves step 03 dataset prep is complete. If the latest passed preflight is newer than the latest CARA-Strong smoke attempt, the dashboard marks step 08 as the current repeat action and keeps step 09 locked until a newer CARA-Strong smoke runs.
- 2026-06-06: Step 08 CARA-Strong rerun after the conditioner patch trained to about step 170/250 and logged CARA metrics, then failed because Azure OOM-killed a PyTorch `pt_data_worker` process (`anon-rss` about 165 GB in `execution-wrapper-7.log`). This confirms the native conditioner patch worked and shifts the failure to DataLoader multiprocessing memory pressure. Patched Stable Audio smoke/full job defaults, dashboard submission, backend event logging, and trainer argument handling to use `num_workers=0`; the trainer now records `dataloader_worker_mode=single_process`. The dashboard also preserves latest attempt vs latest passed smoke evidence, so a newer failed step 08 keeps step 09 locked until the fixed step 08 rerun passes.
- 2026-06-06: First full Stable Audio CARA-Strong fine-tune reached about `6000 / 7665` steps after roughly `6h 45m`, then Azure ML failed the job with `DiskFullError` under `AZ_BATCH_NODE_ROOT_DIR` (`128441 MB` total, `817 MB` available). The timing aligns with accumulated full-run checkpoints near a periodic checkpoint boundary, not a model-configuration failure. Patched the trainer and Azure job specs to use mounted input/output folders and checkpoint retention: keep only `last.ckpt` plus the newest periodic checkpoint by default (`checkpoint_keep_last_n=1`) while preserving full-run progress reporting and existing H100/datastore/environment resources.
- 2026-06-06: Began the matched MusicGen/autoregressive smoke ladder while the diffusion full run is pending. Added CARA suffix-token utilities, strengthened the MusicGen EnCodec cache to write registry/vocab artifacts, added MusicGen preflight/smoke/full Azure job definitions, and wired dashboard/backend steps 07-11 for baseline, CARA-lite, detached suffix probe, and CARA-Strong suffix smoke. The methodology is documented as a like-for-like comparison: same data, same registry, same leakage controls, with architecture-native CARA expression only where diffusion and autoregressive models differ.
- 2026-06-07: Tightened MusicGen preparation disk behavior and dashboard state retention. `05b_prepare_musicgen_dataset.yml` and `06_cache_musicgen_encodec_tokens.yml` now use mounted AzureML input/output folders so prepared AR WAV chunks and EnCodec cache artifacts stay in the datastore path rather than being staged wholesale on node root disk. Azure ML confirmed `brave_whale_wplcgpws3c` completed the MusicGen prep on `cpu-prep-cluster`. The finetuning dashboard now persists model-specific preprocessing progress in browser `localStorage`, clears it on a fresh prep submission, and includes latest Azure preprocessing job status in readiness, so the AR page does not fall back to step 03 merely because the user navigated away or the slower blob-count progress endpoint has not returned.
- 2026-06-07: Scoped the fine-tuning gate refresh by dashboard page variant. The Autoregressive / MusicGen page now requests `readiness?variant=autoregressive`, so it no longer waits on Stable Audio preflight/smoke/full-run status while deciding MusicGen ladder gates.
- 2026-06-07: Added a third fine-tuning page, `Finetune: Hybrid`, for ACE-Step v1.5 planning. Primary-source research confirms ACE-Step v1.5 is an LM-planner plus DiT-synthesizer model, and the official repo points to Side-Step for CUDA LoRA/LoKR training. The new page is scaffold-only and launch-locked: it documents the benchmark testing ladder, planner-survival risk, DiT attribution-head transfer hypothesis, and existing-Azure-resources-only cost guardrail without submitting jobs.
- 2026-06-07: MusicGen step 06 EnCodec cache job failed because `cache_musicgen_encodec_tokens.py` loaded full `MusicGen.get_pretrained`, which instantiated the LM/T5 text conditioner and exposed a Torch/Transformers mismatch in the Azure environment. Patched the cache job to load only AudioCraft's compression model for waveform-to-EnCodec token caching, while preserving each cached row's `attribution_binding` and downstream `cara_suffix_vocab.json` / `cara_registry_resolver.json` generation. Bumped `env-musicgen-audiocraft` to v3 with an explicit Transformers/T5 compatibility preflight so the CARA suffix remains tokenized from the audio-token manifest row rather than from a detached prompt-only path.
- 2026-06-07: Expanded the ACE-Step v1.5 Hybrid page from a scaffold into a 12-step locked benchmark testing ladder. The ladder now separates planner survival, DiT tap discovery, planner-preserved CARA smoke, planner-bypass CARA smoke, and Hybrid CARA-Strong smoke. The runbook now requires planner survival/loss metrics, DiT attribution metrics, registry-valid decoding, shuffled-label baseline, source-disjoint evaluation, and explicit evidence labels before any full hybrid comparison can launch.
- 2026-06-07: A repeated full Stable Audio CARA-Strong run failed again at the same wall-clock point with `UserScriptFilledDisk`. Azure reports the active `gpu-fulltraining-h100` compute as `Standard_NC40ads_H100_v5`, whose listed local resource volume is 131,072 MB; the region also lists `Standard_NC80adis_H100_v5` at 262,144 MB but with two H100 GPUs. Patched the trainer to place common HF/Torch/tmp runtime cache directories under the mounted output folder and to use stable checkpoint filenames (`periodic.ckpt` overwritten plus `last.ckpt`) so periodic checkpoints do not accumulate on node root.
- 2026-06-07: The next full Stable Audio CARA-Strong attempt reached `7665 / 7665` training steps in `std_log-9.txt` and logged final training metrics including `cara/pool_top1=0.875`, `cara/family_top1=0.875`, `cara/weighted_attribution_loss=0.0426`, and `train/loss=0.941`, then Azure ML failed with `DiskFullError` during post-training artifact handling (`128441 MB` total, `1830 MB` available). Patched full-scope Stable Audio training to disable full Lightning checkpoints/progress output and instead write `checkpoints/trainable_delta.pt` into the mounted AzureML output path during training and again at train end. Smoke runs keep normal Lightning checkpoints; full runs now keep a compact trainable-parameter delta backup against the base checkpoint before final Azure packaging.
- 2026-06-07: Added periodic percentage progress to Operations / Azure Runs. The backend now exposes `/api/azureml/job-progress` and `/api/azureml/job-progress/{job_name}` with cached read-only estimators: MusicGen EnCodec cache progress counts non-empty `.pt` token files in the Azure datastore against the expected MusicGen chunk count, and trainer progress uses Azure MLflow step metrics where available. The Azure Runs table now shows a compact progress column and the selected-job card shows percent, observed count, elapsed time, ETA, last observed artifact/metric time, and estimator notes.
- 2026-06-07: Wired the ACE-Step v1.5 Hybrid ladder closer to test runs without launching anything. Added `env_ace_step.yml`, `13_ace_step_env_preflight.yml`, `ace_step_env_preflight.py`, `/api/training/hybrid-readiness`, and `/api/training/ace-preflight`, then registered `azureml:env-ace-step:1` in the existing Azure ML workspace. The Hybrid dashboard now refreshes backend gate state, shows the append-only 12-step ladder, requires typed `LAUNCH ACE PREFLIGHT` confirmation, checks both H100-backed compute targets before submission, and keeps later tensor/planner/DiT smoke stages locked until the ACE preflight passes. The preflight validates CUDA, `diffusers.AceStepPipeline`, common LoRA/training imports, source audio visibility, and CARA label fields including `cara_source_pool_id`.
- 2026-06-07: Added the benchmark testing evaluation readiness layer after the Stable Audio full run completed. The Testing page now consumes `/api/evaluation/readiness`, the Benchmarks page consumes `/api/evaluation/benchmarks`, and both distinguish native CARA-id metrics from external-probe baseline metrics. Added a repairability schema with exact-pool, repairable-pool, family/genre fallback, and unattributable buckets, plus dry-run-only evaluation planning until a dedicated Azure ML evaluation command job is added. The original base model is explicitly marked `N/A` for native CARA-id output and remains comparable through the same external probe, random, prior, keyword, prompt-only, and shuffled-label controls used for the fine-tuned models.
- 2026-06-07: Added an OpenAI-assisted abstract box to the Benchmarks page. `/api/evaluation/benchmark-summary` reads the current benchmark matrix and evaluation goal, uses `.env` `OPENAI_API_KEY`, `OPENAI_MODEL`, and `OPENAI_REASONING_EFFORT`, and saves generated summaries to `evaluation/generated/benchmark_tldr_latest.json` plus append-only `evaluation/generated/benchmark_tldr_log.jsonl`. The saved artifact includes the model/suite snapshot, benchmark payload, prompt messages, usage metadata, and a `generated_prompts` field reserved for any future OpenAI-created prompt sets.
- 2026-06-07: Wired live evaluation wave 1 behind the Testing page dry-run toggle. Unticking dry-run and typing `LAUNCH BENCHMARK TESTING EVALUATION` now submits `azureml/jobs/14_benchmark_testing_stable_audio_eval.yml` on existing GPU compute `gpu-smoke-h100`, with both H100-backed compute targets checked for active jobs first and no CPU fallback. The job validates the prepared manifest and completed Stable Audio trainable-delta artifacts, loads the base Stable Audio checkpoint on CUDA, and saves `evaluation_plan.json`, `prompt_manifest.jsonl`, `control_metrics.json`, and `benchmark_testing_stable_audio_eval_report.json` under the existing datastore evaluation-runs path. Generated-audio native/probe scoring remains a follow-on stage that must reuse the saved prompt manifest.
- 2026-06-07: Renamed user-facing evaluation language from peer-review testing to benchmark testing. The dashboard now reports the latest benchmark setup job separately from full benchmark scoring, so a completed setup/control run is not mistaken for generated-audio benchmark completion.
- 2026-06-07: Added the benchmark prompt-set lock. The dashboard can now lock a completed setup job's `prompt_manifest.jsonl` into `evaluation/generated/benchmark_prompt_set.lock.json`, exposes that state on Testing and Benchmarks, and blocks accidental new setup prompt-sampling runs once the lock exists. Future generated-audio scoring jobs must consume the locked `prompt_manifest_uri` for like-for-like Diffusion, Autoregressive, Hybrid, and baseline comparison.
- 2026-06-07: Wired generated-audio benchmark Step 15 after the locked prompt-set setup. Added `azureml/jobs/15_benchmark_testing_stable_audio_audio.yml`, `src/benchmark_testing_stable_audio_audio.py`, and the `/api/evaluation/audio-benchmark/run` live submission path. The Testing page's Generated-Audio Benchmark panel now supports a 20-row smoke run or full locked-set Diffusion benchmark using seed `0`, submits only on existing H100-backed Azure ML compute, mounts the locked `prompt_manifest.jsonl` as a `uri_file`, saves WAVs plus `generation_manifest.jsonl` and `benchmark_audio_metrics.json`, and records attribution metrics as pending rather than inferring CARA accuracy from expected labels.
- 2026-06-07: Fixed Testing page reload/navigation state for generated-audio benchmarks. The backend now exposes any active Step 15 Azure ML job in evaluation readiness and rejects duplicate live audio benchmark submissions while a Step 15 job is active. The Testing page shows the active job name/status/output folder, links to Azure Studio, and disables the live submit button on return to the page instead of resetting into a resubmittable state.
- 2026-06-07: Fixed Benchmarks result persistence for generated-audio evaluation. The backend now resolves the latest completed Step 15 generated-audio job separately from the latest submitted event, so a canceled or running duplicate does not replace the last completed result. The Benchmarks page now shows the completed audio benchmark lanes, suites, WAV artifact count, generation manifest path, and explicit `attribution pending` status until native/probe scoring produces replacement metrics.
- 2026-06-07: Added Step 16 `Attribution Scoring` to the Testing dashboard ladder. The new Azure job `16_benchmark_testing_stable_audio_score.yml` consumes the latest completed Step 15 generated-audio output folder, reads `generation_manifest.jsonl`, scores only real native/probe CARA prediction fields through registry/family checks, writes `metrics_latest.json`, and lets the Benchmarks page replace pending matrix cells when those metrics exist. The scorer deliberately leaves cells pending when prediction fields are absent, avoiding circular use of expected labels.
- 2026-06-08: Step 16 attribution scoring job `brave_cake_cfrj2gxlpq` completed against full generated-audio run `clever_knot_qk2pwmx6qh` and produced `metrics_latest.json` for 640 generated WAV rows. The result correctly reported `missing_predictions` for base external probe, diffusion native, and diffusion external probe because `generation_manifest.jsonl` did not yet contain real predicted CARA fields. Patched the dashboard to label Step 16 as `Attribution scoring` and show `missing predictions` in the matrix instead of generic `pending`, making the remaining extractor/probe gap explicit.
- 2026-06-08: Wired the actual Stable Audio native CARA attribution extractor into Step 16. `benchmark_testing_stable_audio_score.py` now mounts the completed CARA-Strong trainable-delta output, reloads the fine-tuned Stable Audio model on the existing H100 GPU compute, replays diffusion prompt/seed rows from the generated-audio manifest, taps DiT generation hidden states, loads the saved `cara_attribution_head` weights, decodes pool/family predictions through the registry resolver, and writes `native_predictions.jsonl`, `scored_generation_manifest.jsonl`, `cara_registry_resolver.json`, and updated `metrics_latest.json`. The scorer still refuses to infer predictions from expected labels; base native remains N/A and external probe remains pending until a separate probe is wired.
- 2026-06-09: Upgraded benchmark attribution scoring and dashboard reporting for both Stable Audio and MusicGen. Stable Audio Step 16 and MusicGen scoring now route native/probe predictions through the shared CARA repairability resolver instead of hand-counting exact matches only. `metrics_latest.json` now carries a `repairability_matrix`, tier counts, prediction examples, and per-lane repairability summaries for exact pool, repairable pool, family/genre fallback, and unattributable outcomes. The backend now prefers live datastore metrics over stale local cache, merges family-specific scoring results, and exposes the repairability matrix plus prediction examples to the Benchmarks page. The Testing page explains the Step 16 scoring output contract and includes an explicit `Force re-score current full run` control for intentional scorer reruns after code changes. The Benchmarks page displays a separate repairability ladder matrix so missing predictions, unattributable predictions, repairable CARA IDs, and exact pool matches are visibly distinct.
- 2026-06-09: Stable Audio Step 16 scorer job `mighty_grape_f463c281v6` failed before model loading because the Azure command job uploads `code: ../../src`, while the shared repairability resolver was only available in the repo-level `evaluation/` package. Azure traceback: `ModuleNotFoundError: No module named 'evaluation'` at `from evaluation.cara_repairability import ...`. Added an Azure-packaged fallback copy at `src/cara_repairability.py` and patched both Stable Audio and MusicGen scoring scripts to import from `evaluation.cara_repairability` locally or fall back to `cara_repairability` inside Azure's `src` code directory. Verified source compilation and the fallback import path from a `src`-only Python path.
- 2026-06-10: Tightened benchmark repairability semantics after inspecting the Benchmarks page matrix. Earlier scoring artifacts could count `repairable_pool` as "raw prediction repaired uniquely to some registry pool", which is useful diagnostic evidence but not the same as correct attribution. The resolver now reports separate raw resolution tiers, repair-method counts, and correctness tiers. The repairability ladder matrix now counts `repairable_pool` only when the repaired pool matches the expected held-out pool, keeps `pool_recovered_accuracy` as exact plus correctly repaired, and exposes a separate repair-method matrix plus richer prediction examples (`predicted`, `resolved`, `expected`, method, distance, correctness flags, and top-k candidates when emitted). Existing pre-fix metrics should be re-scored before making claims from repairability percentages.
- 2026-06-10: Added a new `Finetune: Context Diffusion` sidebar page after the original diffusion branch. This is an append-only follow-on experiment inspired by Context Diffusion: reuse the completed Stable Audio CARA-Strong ladder as inherited evidence, then track new context-pack, context-cache, context-conditioner preflight, context smoke, full context fine-tune, and context benchmark stages. The branch is launch-locked until those Azure jobs exist. Its methodology requires audio context examples to be first-class conditioning tokens beside text, not prompt tags; controls must include context plus prompt, context-only, prompt-only, shuffled-context, mismatched-context, and no-context lanes.
- 2026-06-10: Added a `Documentation` sidebar group below Evaluation with a `Runbook & Logs` page. The page reads the live `docs/cara_finetuning_runbook.md` and `docs/EXPERIMENT_LOG.md` files through a whitelisted read-only backend API and renders them in the dashboard with refreshable file metadata. This keeps the evolving methodology and append-only experiment evidence visible inside the console without duplicating markdown content into the frontend.
- 2026-06-10: Made the first Context Diffusion branch stages submittable. Added Azure jobs and backend/frontend wiring for step 10 context-pack lock (`10_prepare_stable_audio_context_packs.yml`), step 11 context metadata/cache validation (`11_cache_stable_audio_context_metadata.yml`), and step 12 context conditioner preflight (`12_stable_audio_context_preflight.yml`). These stages reuse the completed Stable Audio prepared manifest, create source-disjoint context/control manifests, validate context audio references, and record the planned Context Diffusion lanes. Step 13 context smoke remains intentionally locked until the Stable Audio context conditioner/training wrapper actually consumes context tokens in DiT cross-attention; this avoids creating false evidence from a smoke run that ignores context.

- 2026-06-10: Fixed Context Diffusion step 10 gating. The branch now uses cached local completion evidence for the inherited Stable Audio full run (`modest_arch_clgnkqrz4z` and the locked trained-model URI already used by benchmark testing) rather than blocking step 10 on a slow live Azure status read. New Context Diffusion jobs still need to submit and complete normally before later branch steps unlock.

- 2026-06-10: Corrected Context Diffusion compute routing. Steps 10-12 now follow the H100-preferred preprocessing policy: check both `gpu-smoke-h100` and `gpu-fulltraining-h100` for active jobs, submit to `gpu-smoke-h100` when free, and fall back to `cpu-prep-cluster` only when H100-backed compute is unavailable. The static Azure YAML defaults were also changed from CPU to `gpu-smoke-h100` so manual submissions do not silently use CPU.

- 2026-06-10: Diagnosed Context Diffusion step 10 job `busy_crowd_qgft2fppb0`: Azure showed it `Running` on `gpu-smoke-h100`, but blank `std_log.txt` was expected because the original context-pack script printed only at completion. The script also had an accidental quadratic full-manifest scan while building shuffled/mismatched controls. Replaced per-target full scans and sorts with indexed deterministic selection, added progress logs every 5,000 rows, and verified a 1,200-row fixture completed in about one second with source-disjoint context packs and all expected artifacts.
- 2026-06-10: After Context Diffusion step 10 completed, rechecked steps 11-13 for inherited step-10 failure modes. Step 11/12 already avoid the quadratic context-selection path, but they now print live Azure `std_log.txt` progress, write JSON-safe artifacts through the shared sanitizer, and report compute as `h100_preferred_else_cpu` because the dashboard may route them to H100 or CPU depending on live H100 availability. Step 13 remains intentionally locked until a real Stable Audio context conditioner/training wrapper consumes audio context tokens in DiT cross-attention; this prevents a false smoke pass from a trainer that ignores context.
- 2026-06-10: Documented the Context Diffusion method transfer from image diffusion to Stable Audio. The branch does not treat audio as literal image pixels; it converts selected source-disjoint audio examples into frozen audio-latent/context tokens and concatenates them beside normal text/time conditioning. Expected benchmark evidence is improved pool top-k, repairability, and family fallback for relevant context, with clear degradation on shuffled-context and mismatched-family controls.
- 2026-06-10: Step 12 Context Diffusion preflight completed and exposed the next implementation gap: Step 13 had been correctly locked because no context-smoke Azure job existed. Added `azureml/jobs/13_stable_audio_context_smoke.yml`, `src/stable_audio_context_smoke.py`, backend submission/status wiring, and a dashboard launch button. Step 13 is now a GPU-only context-conditioner smoke that consumes the locked context packs/cache and scores context-plus-prompt, context-only, prompt-only, shuffled-context, and mismatched-family lanes. Its claim scope is explicitly narrower than full Stable Audio DiT context fine-tuning; that remains Step 14 work.
- 2026-06-10: After Step 13 passed quickly, implemented Step 14 as the first full Context Diffusion branch rather than treating the smoke as persistence proof. `smoke_stable_audio_trainer.py` can now run with `--context_mode metadata_context_conditioning`, validates Step 10/11/13 artifacts, enriches each prepared manifest row with source-disjoint context-pack fields, exposes those fields through the Stable Audio metadata module, and adds `cara_context_pool_index`, `cara_context_pool_family_index`, `cara_context_policy_index`, and `cara_context_count` as Stable Audio Tools integer conditioners in DiT cross-attention. Added `azureml/jobs/14_full_stable_audio_context_trainer.yml`, backend/frontend Step 14 launch wiring, typed confirmation, and the same disk-safe full-run checkpoint strategy used by the successful Stable Audio full run: GPU-only `gpu-fulltraining-h100`, `num_workers=0`, mounted output, no accumulating Lightning full checkpoints, and a single overwritten `checkpoints/trainable_delta.pt`. Claim scope remains metadata-context conditioning, not raw audio-context latent cross-attention.
- 2026-06-11: Generalised the training progress dashboard path so new branches do not disappear from progress tracking. `/api/training/run-progress` now accepts `model=diffusion`, `model=context_diffusion`, `model=musicgen`, `model=latest`, or `model=all`, selects the latest or active submitted trainer event from `registry/cara_strong/azure_training_jobs.jsonl`, reads fast datastore `logs/*/metrics.csv` artifacts for Stable Audio-derived trainers, and uses MLflow only as a compatible fallback. The Context Diffusion page now has a Step 14 progress card showing observed/max steps, percent, latest loss, elapsed time, ETA, metrics source, and Azure Studio link without touching or restarting the cloud job.
- 2026-06-12: Clarified the Context Diffusion handoff from fine-tuning to evaluation. After Step 14 completes, the Context Diffusion ladder now marks Step 15 as the active benchmark handoff and shows a button that opens the Testing page. The generated-audio benchmark and attribution scoring remain in the shared Testing workflow so Context Diffusion uses the same locked prompt set, seed controls, Step 15 audio generation, and Step 16 scoring path as the Diffusion and MusicGen comparisons.
- 2026-06-10: Added the Context Diffusion full model lane to benchmark testing while the new fine-tune is running. The Testing page now includes `context_diffusion_cara_strong_full` in the same locked-prompt generated-audio process as Diffusion and MusicGen; Stable Audio Step 15 and Step 16 now mount both the original Diffusion trainable delta and the Context Diffusion trainable delta. The Benchmarks page foregrounds candidate lanes (`Diffusion CARA`, `Context Diffusion`, `MusicGen CARA`, and retrieval) instead of released/base diffusion columns. Repairability reporting now rejects old loose-cache semantics unless a metrics artifact explicitly declares `correctness_semantics=expected_label_correctness`; this prevents checksum repair to the wrong registry pool from being displayed as correct pool attribution.
- 2026-06-08: Diagnosed MusicGen EnCodec cache job `lime_chain_hb4j796gry` as failed during final manifest serialization after writing 40,788 token `.pt` blobs. Root cause was a NumPy/PyTorch scalar such as `int64` inside `cached_rows`, which Python's default `json.dumps` cannot serialize. Patched `cache_musicgen_encodec_tokens.py` with JSON-safe scalar conversion, safe summary/resolver/vocab writes, and resumable cache recovery via `--existing_cache_dir` plus `--resume_existing true`, so rerunning step 06 can reuse existing token files and write the missing `manifest.encodec.jsonl`, `encodec_cache_summary.json`, `cara_registry_resolver.json`, `cara_suffix_vocab.json`, and report artifacts. The MusicGen token-cache gate now also checks the final summary artifact so completed cache outputs can be recognized independently of stale Azure status.
- 2026-06-08: Fixed the MusicGen trainer preflight/reporting crash seen in Azure job `f2ee3dcd80234a6ba766e628a4d9389b`. The shared `base_metadata()` helper expects `compute`, `dashboard_triggered`, and `report`; `smoke_musicgen_ar_trainer.py` was incorrectly passing `status` and `compute_target`, causing the job to fail after the preflight body reached report generation. Patched the metadata call and verified the script now exits through the normal report path.
- 2026-06-08: Fixed the follow-on MusicGen preflight report serialization crash from Azure job `0b69ae436603412fae5b44a7d6df6d55`. `test_prep_common.write_report()` now recursively converts NumPy/PyTorch scalar-like values, tensors, paths, sets, tuples, and `.item()` / `.tolist()` objects into JSON-safe primitives before writing reports, metadata, aliases, and console output. This makes report writing robust across MusicGen token-cache/preflight/smoke and the Stable Audio/benchmark scripts that share the same helper.
- 2026-06-08: Reframed and rewired the Autoregressive / MusicGen ladder to match the Stable Audio full-run claim. The old `smoke_musicgen_ar_trainer.py` proxy remains available for historical/debugging evidence, but the active step 07/08/12 Azure jobs now call `musicgen_lm_cara_trainer.py`, which loads the real AudioCraft `facebook/musicgen-small` LM, trains against cached EnCodec token targets through `lm.compute_predictions`, taps the real LM transformer hidden state, adds the CARA suffix head/loss for `cara_probe` and `cara_strong`, and writes an overwrite-style `checkpoints/musicgen_lm_cara_delta.pt` trainable delta to the mounted Azure output path. The dashboard Step 12 now launches the full real MusicGen LM CARA-Strong fine-tune only after the real-LM smoke ladder passes.
- 2026-06-08: Tightened MusicGen readiness so historical proxy smoke/preflight runs cannot unlock the real-LM ladder. New MusicGen preflight/smoke/full submissions record `trainer_implementation=real_audiocraft_musicgen_lm`, and the dashboard only marks steps 07-11 passed when the completed Azure job has that marker, the expected environment, and matching serialized command inputs.
- 2026-06-08: Real-LM MusicGen Step 08 baseline smoke job `elated_tangelo_sm8ml7ms99` failed on the first AudioCraft transformer forward with `RuntimeError: expected scalar type Float but found Half`. The report confirmed the data path and registry were valid (`40,788` cached EnCodec rows, `98` pools, `9` families, first batch `[8, 4, 512]`), so the failure was an LM runtime dtype/batch-size issue rather than a CARA manifest issue. Patched `musicgen_lm_cara_trainer.py` to cast the loaded LM consistently to `float32`, disable CUDA autocast around `compute_predictions`, force the public PyTorch AudioCraft transformer runtime path, and run a one-batch forward check during Step 07 preflight. MusicGen real-LM submissions are now clamped to batch size `2` and record `model_dtype=float32`.
- 2026-06-08: Real-LM MusicGen Step 08 baseline smoke job `magenta_pencil_4tz7r7v7n5` completed successfully after the dtype/batch-size patch. Downloaded `std_log-14.txt` reports `status=passed`, `test_name=08_musicgen_lm_cara_trainer`, `variant=no_cara_baseline`, `trainer_implementation=real_audiocraft_musicgen_lm`, `global_step=250`, first batch shape `[2, 4, 240]`, `model_dtype.active=torch.float32`, and `train_loss/audio_token_loss=2.859896478176117`. Because previous MusicGen Step 09-11 rows predated the real-LM marker, they remain historical/debug evidence only and must be rerun as real-LM Step 09 CARA-lite, Step 10 suffix-probe, and Step 11 CARA-Strong before Step 12 full MusicGen fine-tuning.
- 2026-06-08: Real-LM MusicGen smoke ladder reruns passed on `gpu-smoke-h100` with `env-musicgen-audiocraft:3`, batch size `2`, and `model_dtype=float32`. Step 09 `great_fork_r6mptsdbqx` (`cara_lite`) used `optimizer_mode=musicgen_lm_audio_only`, visible CARA prompt text, `global_step=250`, `train_loss=2.855820629477501`, and `cara_suffix_loss=0.0`, confirming the prompt-control lane has no native suffix supervision. Step 10 `careful_spider_zbnnnw28gc` (`cara_probe`) used `optimizer_mode=musicgen_lm_frozen_suffix_head_only`, `trainable_parameter_count=1374427`, `cara_suffix_loss=3.7556205444335937`, and latest suffix decode metrics `registry_valid=1.0`, `hierarchical_valid=1.0`, `checksum_valid=1.0`, `suffix_exact=0.0`. Step 11 `shy_fennel_ldvntxd3nl` (`cara_strong`) used `optimizer_mode=musicgen_lm_plus_cara_suffix_head`, `trainable_parameter_count=420958427`, `cara_suffix_loss=3.647953637123108`, and the same registry-valid/hierarchical/checksum metrics with `suffix_exact=0.0`. This confirms the CARA suffix is generated from the real MusicGen LM hidden state tied to cached EnCodec audio-token examples, and that full Step 12 is now justified; exact suffix accuracy remains a full-training evaluation target rather than a smoke-pass requirement.
- 2026-06-08: Full real-LM MusicGen Step 12 job `teal_calypso_2cbdxynnc6` failed during `trainer_fit` when the full DataLoader later sampled `tokens/train/freesound_97721_00000.pt`, which contained fewer than two EnCodec frames. The report had already confirmed valid real-LM setup (`train_rows=33225`, `encodec_manifest_rows=40788`, first batch loaded, `optimizer_mode=musicgen_lm_plus_cara_suffix_head`, and `trainable_parameter_count=420958427`), so this is a cache hygiene failure rather than a broken CARA suffix or LM integration. Patched `musicgen_lm_cara_trainer.py`, MusicGen trainer YAMLs, dashboard materialization, and tests so preflight/smoke/full runs filter `encodec_frame_count < 2` before train/eval selection, record `encodec_rows_retained`, and write `rejected_encodec_frame_rows.json`. Rerun Step 12 without recaching unless the rejected-row count is unexpectedly large.
- 2026-06-12: Updated the shared Testing page generated-audio launcher so Context Diffusion's Step 15 benchmark handoff can run one model, several models, or all candidate models against the same locked prompt set. The UI now exposes explicit Diffusion-only, Context-only, MusicGen-only, and All-model selectors plus per-lane checkboxes; the backend accepts the neutral typed confirmation `LAUNCH AUDIO BENCHMARK` while preserving the previous `LAUNCH ALL MODELS AUDIO BENCHMARK` phrase for compatibility. This keeps the benchmark process reusable for side-by-side runs and for staged reruns where only a newly completed model lane needs audio generation/scoring.
- 2026-06-12: Tightened the Testing page live generated-audio gate after a Context-only full benchmark was able to launch without the user freshly typing the visible confirmation phrase. The active Azure job was left running. The frontend now clears the confirmation field whenever dry-run/live mode, scope, model lanes, suite selection, or prompt limit changes, and after a successful live submit. The top-level backend `/api/evaluation/audio-benchmark/run` endpoint now accepts only the exact current phrase `LAUNCH AUDIO BENCHMARK` for live submissions; legacy phrases remain accepted only inside internal child-job/retry compatibility paths. Verified that `dry_run=false` with an empty confirmation returns HTTP 409 before Azure submission.
- 2026-06-12: Added selected model-lane controls to Step 16 attribution scoring so a completed source audio run can be scored for one lane, several lanes, or every lane present without redoing previous scoring jobs. The backend now validates requested scoring `model_ids` against the selected source audio run, maps them to the Stable Audio or MusicGen scoring families, serializes `model_ids` into the Azure command job, and cancels mismatched submissions. Stable Audio and MusicGen scorer scripts now filter `generation_manifest.jsonl` by selected model IDs before writing native predictions, scored manifests, metrics, and repairability matrices. Scoring continues to write new output folders and does not overwrite generated audio or old scoring artifacts.
- 2026-06-12: Context-only Step 16 scoring job `shy_candle_52ch4jrfbb` failed after correctly filtering the manifest to 320 `context_diffusion_cara_strong_full` rows because the scorer mounted the static Context Diffusion parent folder (`.../stable_audio_context/context_full/`) instead of the completed full-run child folder containing `checkpoints/trainable_delta.pt`. Patched generated-audio and attribution-scoring materializers to resolve the latest `stable_audio_context_full_submitted` registry output path (`.../context_full/cara-stable-audio-context-cara-strong-full-20260610-122538/`), pass it as `context_trained_model_data`, expose it in Step 16 dry-run preflight, and record it in scoring events.
- 2026-06-12: Context-only Step 16 rerun completed and loaded the Context Diffusion trainable delta, but the native prediction examples all had `prediction_status=exception`, no predicted pool, no top-k candidates, and `native_extraction.trainable_delta.unmatched_preview` included context conditioner and attribution-head tensors. Treated this as an evaluation/extractor reconstruction problem rather than a defensible model failure until the exception is surfaced and fixed. Patched benchmark aggregation to merge latest labelled evidence per lane, so Context-only reruns no longer overwrite prior Diffusion/MusicGen scored lanes with `no_labelled_rows`, and patched matrix display so `0/0` is shown as `no labelled rows` rather than a real percentage.
- 2026-06-12: Tightened Stable Audio generated-audio and attribution-scoring reconstruction after the 0% Diffusion/Context Diffusion audit. Step 15/16 now rebuild the CARA conditioner graph, and Context Diffusion also rebuilds the context metadata conditioner graph, before applying `checkpoints/trainable_delta.pt`; critical unmatched CARA/context tensors now fail the job rather than silently running the base model. Step 15/16 also pass structured CARA/context metadata from the locked benchmark row into generation replay for label-applicable lanes, while suppressing that metadata for open-quality and shuffled-label controls. The scorer now reports all-native-exception lanes as `extractor_failed` and partial failures as `scored_with_extractor_errors`, with error examples, instead of flattening extractor exceptions into clean 0% attribution.
- 2026-06-12: Investigated the post-fix `extractor_failed` Stable Audio benchmark state. Cached Step 16 metrics showed both Diffusion and Context Diffusion deltas were reconstructed successfully with zero critical unmatched CARA/context conditioner tensors, but every native prediction failed with `No CARA hidden-state tap produced a batch-aligned feature tensor`. Patched the generated-audio native extractor to keep training-time tap strictness while accepting generation-time branch expansion: duplicated DiT hidden-state tap rows, such as guidance branches, are averaged per requested sample and recorded in each prediction's `feature_alignment` audit field.
- 2026-06-12: Clarified benchmark repairability after the first post-extractor-fix side-by-side results. The strict repairability ladder continues to count only recovery against the expected held-out prompt label, while the new mechanical resolution matrix separately reports whether emitted CARA-like strings resolved to any valid registry pool. This preserves the stronger benchmark claim while still surfacing candidate influence evidence when generated audio plausibly drifts from the prompt target. The dashboard now labels prediction examples with both correctness tier and mechanical tier, and documents the current repair thresholds: checksum same-payload repair, unique pool payload edit-distance <= 2, and family fallback <= 4.
- 2026-06-12: Added a stronger dashboard/runbook caveat for mechanical resolution. Diffusion and Context Diffusion native attribution heads are closed-set classifiers over locked registry pool indices, so a 100% mechanical exact-registry rate mainly means the extractor emitted valid class indices; it is not itself pool-correct attribution. MusicGen's autoregressive suffix lane must decode or repair an emitted code sequence into the registry, so mechanical resolution is more informative there. The strict repairability ladder remains the target-correct benchmark.
- 2026-07-01: Added a future-refinement note for a possible CARA calibration / reinforcement-retuning branch. The proposed branch would test whether a reward or preference pass can increase exact codeword recovery while preserving audio-linked attribution evidence. It must reuse the locked benchmark prompt set, compare pre-retune versus post-retune metrics, keep strict controls for prompt-only/shuffled-label shortcuts, preserve the supervised checkpoints, and avoid claiming stronger attribution unless audio-hidden-state controls prove the improvement is not merely better id printing.
- 2026-07-01: Narrowed the Hybrid ACE-Step branch to the first comparable 0.6B-planner target. The ACE preflight job, backend readiness payload, and Hybrid dashboard now record `ACE-Step/acestep-5Hz-lm-0.6B` plus the base/SFT DiT target as the selected side-by-side architecture. Larger ACE-Step planner variants remain future scale-ablation branches, not the first Hybrid CARA-Strong comparison.
- 2026-07-01: Implemented the runnable Hybrid ACE-Step ladder after the preflight gate. Added Azure ML jobs for Step 03 tensor preparation, Step 04 planner survival probing, Step 05 DiT tap discovery, Steps 06-11 Hybrid smoke variants, and Step 12 full Hybrid handoff; added `src/ace_step_hybrid_stages.py`; wired backend launch/status routes; and replaced the Hybrid dashboard placeholders with ordered gated launch buttons. Step 03/04 are H100-preferred but CPU-capable, while Step 05/06-12 are GPU-only and check both H100-backed compute targets before submission. The full stage records a Side-Step handoff contract and only claims a deployable ACE adapter when `run_sidestep=true` with mounted ACE checkpoints/tensors; the default dashboard path remains a non-deployable contract adapter for plumbing and methodology validation.
- 2026-07-01: Fixed the Hybrid Step 01 source/license review gate so it is no longer a dead-looking disabled ladder item. The dashboard now exposes `01 Record Source + Licence Review`, which writes `registry/cara_strong/ace_step_source_license_review.json` and a local registry event without submitting Azure compute. Step 02 ACE preflight now explicitly requires that local review record, plus the existing manifest lock and Azure upload confirmation.
- 2026-07-01: Fixed the sidebar completion tick for the Context Diffusion fine-tune. The full context run was already recorded in `registry/cara_strong/azure_training_jobs.jsonl` as `stable_audio_context_full_submitted` and exposed by `evaluation.benchmark_spec.model_lanes()` as `context_diffusion_cara_strong_full`, but `/api/sidebar/completion` only emitted completion items for the original Diffusion and Autoregressive pages. The sidebar now emits `finetune-context-diffusion` using the same model-lane evidence path, so the completed Context Diffusion branch can show its green tick.
- 2026-07-01: Made ACE-Step Hybrid Step 03 visibly a JSON-mode dataset-preparation gate, not just a tensor-prep placeholder. The upstream Side-Step dataset-preparation guide supports folder-only mode and JSON mode; CARA uses JSON mode so every sample can keep tag-withheld captions, a per-sample CARA codeword `custom_tag` for explicit control lanes, and preserved CARA registry metadata. `src/ace_step_hybrid_stages.py` now writes a full ACE-Step `dataset.json` with `metadata` and `samples`, audits Side-Step-supported audio extensions, preserves the CARA label fields in both `dataset.json` and `ace_tensor_manifest.jsonl`, updates the Side-Step preprocess command contract to use `--dataset-json`, and the Hybrid dashboard/ladder labels Step 03 as `Prepare ACE Dataset JSON + Tensors`.
- 2026-07-01: Extended Hybrid Step 03 dataset preparation to convert incompatible source audio formats with `ffmpeg` rather than failing on `.aif` / `.aiff` or other decodable-but-not-Side-Step-native files. Side-Step-native formats remain `.wav`, `.mp3`, `.flac`, `.ogg`, `.opus`, and `.m4a`; anything outside that set is converted to 48 kHz stereo WAV under `converted_audio/` in the Step 03 output folder. The ACE `dataset.json` points to the converted WAV, while `original_audio_path` and `audio_conversion` preserve lineage for audit and peer-review reproduction.
- 2026-07-01: Fixed the Hybrid Step 02 launch UX. Backend readiness already reported `preflight_enabled=true` after Step 01 was recorded, but the frontend kept the Step 02 button disabled until the exact typed phrase was present, making the open gate look locked. The button now becomes clickable when the backend preflight gate is open and still enforces `LAUNCH ACE PREFLIGHT` with a visible error/log message before submitting the live Azure job.
- 2026-07-01: Prepared the Hybrid ACE-Step sidebar completion state for the eventual full-run result. `/api/sidebar/completion` now emits `finetune-hybrid` using the ACE ladder's Step 12 `ace_step_hybrid_full_submitted` completion evidence and output URI, so the sidebar will show the same green completed tick pattern as Diffusion, Context Diffusion, and Autoregressive after the full Hybrid comparison run has actually completed.
- 2026-07-01: Applied the disk-saving trainable-delta rule to the Hybrid ACE-Step full stage. Step 12 now tags the Azure job with `cara_checkpoint_strategy=mounted_output_trainable_delta_only` and always writes the canonical `checkpoints/trainable_delta.pt` artifact. The default contract path records a compact Hybrid contract delta; the future `run_sidestep=true` path records Side-Step LoRA/adapter delta artifacts and hashes without writing a merged full ACE-Step checkpoint.
- 2026-07-01: Fixed a second Hybrid Step 02 UX blocker. Backend readiness showed `preflight_enabled=true` after Step 01, manifest lock, and Azure upload confirmation, but the top benchmark ladder still rendered every stage except Step 01 as disabled. The ladder buttons now mirror the lower launch panel: Step 02 is clickable when ACE preflight is open, later stages become clickable only when their respective backend gates are enabled, and Step 02 still requires the typed `LAUNCH ACE PREFLIGHT` phrase before Azure submission.
- 2026-07-01: Fixed the first ACE-Step Step 02 Azure failure mode. The preflight script imported `torch` at module import time, so a broken/missing PyTorch CUDA install crashed at line 11 before writing a diagnostic report. Torch is now imported through the normal checked-import preflight path, so missing Torch/CUDA is reported in `report.json` rather than truncating the job log. The ACE environment was bumped to `env-ace-step:2` and ACE job submission now registers/updates that environment version from `azureml/environments/env_ace_step.yml` before submitting Step 02 or later ACE stages.
- 2026-07-01: Fixed the follow-on ACE-Step image-build failure pattern. Azure showed a failed environment/image build child job while the GPU preflight stayed in `Preparing`, which means the command job was waiting for an image that never built. The optional Side-Step Git install was removed from the base ACE preflight environment and the branch was bumped to `env-ace-step:3`; Side-Step remains a runtime availability check for the future deployable adapter path rather than a mandatory dependency for Step 02 environment validation.
- 2026-07-01: Fixed the next ACE-Step Step 02 preflight failure from `nice_spoon_bhf43qmxr1`. The report showed two distinct blockers: the conda PyTorch image imported `torch` with `libtorch_cpu.so: undefined symbol: iJIT_NotifyEvent`, and the preflight incorrectly required prepared-manifest fields (`cara_pool_id`, `cara_pool_index`, `cara_pool_family_index`) in the raw uploaded `cara_pool_manifest_v2.jsonl`, which only contains `cara_source_pool_id` plus family labels. The ACE environment was bumped to `env-ace-step:4` using Python 3.10 and official CUDA PyTorch pip wheels, while Step 02/03 now normalize raw manifest rows into the CARA training label contract and write the derivation audit into ACE `dataset.json` / `ace_registry_resolver.json`.
- 2026-07-02: Fixed the Hybrid Step 02/03 gate follow-up after `cool_kiwi_4bh1gt91ql`. The dashboard kept Step 02 visually launchable after a passing preflight because readiness checked only that the preflight was not active; it now also requires that the preflight has not already passed, so Step 02 becomes complete/disabled and Step 03 is the active gate. Step 03 itself processed 24,273 source rows, derived 98 pool labels / 9 families, converted 2,261 unsupported audio files with `ffmpeg`, and failed only because 8 rows had no matching audio file. ACE dataset preparation now rejects missing-audio rows into `rejected_audio_rows.jsonl`, skips them from `ace_tensor_manifest.jsonl` and `dataset.json`, and passes when the remaining dataset is valid.
- 2026-07-02: Reviewed the downstream ACE-Step Hybrid ladder before rerunning Step 03. Step 03 now also rejects failed `ffmpeg` conversions into `rejected_audio_rows.jsonl` instead of failing the whole corpus when valid rows remain. Steps 04-12 now validate `ace_tensor_manifest.jsonl` for the required CARA label fields before planner, tap, smoke, or full-stage work begins, and Step 12 treats a successful Side-Step command with no adapter artifacts as failed rather than writing a misleading deployable delta. Added focused tests for conversion rejection, manifest-label validation, and empty-adapter Side-Step failure.
- 2026-07-02: Clarified the ACE-Step Hybrid dashboard after Step 11 passed but the launch UI looked contradictory. The upper card is now labelled as the Hybrid stage ladder, while the lower card is only live launch controls / typed-gate policy. The backend also exposes an active ladder reason so Step 12 can say it is unlocked while still requiring the explicit `LAUNCH ACE FULL FINE-TUNE` confirmation before Azure submission.
- 2026-07-02: Audited the unexpectedly fast ACE Step 12 run `eager_ocean_43f9q2mbwq`. It completed the non-deployable Hybrid contract-adapter fallback in under a minute, not a real ACE-Step Side-Step LoRA fine-tune: the report showed `deployable_ace_adapter=false`, `sidestep.available=false`, `evidence_mode=hybrid_contract_adapter_not_deployable`, one training row, and a 2 KB `hybrid_contract_head_delta`. The dashboard/backend now require `run_sidestep=true` for the full ACE launch and no longer count a contract-only Step 12 completion as Hybrid fine-tune completion.
- 2026-07-02: Reviewed the first honest ACE Step 12 full-run failure `mango_bottle_9wvf5w7nlf`. The run reached the H100 and validated the prepared CARA tensor manifest (`24,265` rows, `98` pools, `9` families), but failed in `0.579s` because `sidestep_engine` / the `sidestep` trainer was not installed in `env-ace-step:4`. Added a dedicated Side-Step full-training environment `azureml:env-ace-step-sidestep:1`, updated Step 12 to use that environment, and changed the generated Side-Step command to the current `sidestep train ...` CLI form. The next expected blockers, if present, are mounted ACE checkpoint and Side-Step tensor paths rather than CARA manifest binding.
- 2026-07-02: Added a pre-submit mounted-input guard for ACE Step 12. The Hybrid dashboard and `/api/training/ace/full` now require `checkpoint_dir` and `sidestep_tensor_dir` to be real `azureml://datastores/ds_cara_raw_audio/paths/...` URI-folder inputs and probe both datastore prefixes for blobs before submitting the H100 job. The frontend no longer sends placeholder `/mnt/azureml/...` paths; it displays checkpoint/tensor ready-or-missing status so a missing ACE bundle or Side-Step tensor export is caught before Azure job preparation.
- 2026-07-02: Added ACE Step 12a, `Prepare Side-Step Inputs`, as the corrective bridge before the full Hybrid run. The dashboard now exposes `/api/training/ace/sidestep-inputs`, backed by `azureml/jobs/23a_prepare_ace_step_sidestep_inputs.yml`, to materialize the public `ACE-Step/Ace-Step1.5` checkpoint bundle into `ace_step/checkpoints/` and run Side-Step preprocessing into `ace_step/tensors/sidestep_tensors/`. Because the visible public checkpoint tree contains `acestep-v15-turbo` plus shared `vae/` and `Qwen3-Embedding-0.6B/`, the active Side-Step path now labels the deployable Hybrid branch as `model_variant=turbo` / `dit_variant=turbo_dit` rather than `base_or_sft_dit`.
- 2026-07-02: Fixed the first ACE Step 12a image-build failure. Azure failed before running `ace_step_hybrid_stages.py` because pip cloned Side-Step and tried to build `flash-attn` from source during dependency resolution, but Torch was not importable inside the isolated build step (`ModuleNotFoundError: No module named 'torch'`). The Side-Step environment is now `azureml:env-ace-step-sidestep:2`, which installs CUDA 12.8 Torch/torchaudio/torchvision and the upstream prebuilt `flash-attn` wheel explicitly, then installs Side-Step with dependency resolution disabled. Step 12a and Step 12 now target that v2 environment.
- 2026-07-02: Fixed the follow-up ACE Step 12a image-build failure from `env-ace-step-sidestep:2`. Azure's conda-file pip section rejected `--no-deps` as an invalid requirements entry before any package install occurred. The Side-Step environment is now `azureml:env-ace-step-sidestep:3`: the image contains the CUDA/Torch/flash-attn dependency stack, while `src/ace_step_hybrid_stages.py` moved Side-Step loading into job runtime diagnostics. This moved remaining blockers from Azure image build into normal Step 12a report diagnostics.
- 2026-07-02: Fixed the next ACE Step 12a normal-runtime failure from `amusing_brake_h1st14ff71`. The job reached H100 execution, downloaded the public `ACE-Step/Ace-Step1.5` checkpoint bundle, and installed the Side-Step wheel, but the generated `sidestep` console script failed immediately with `ModuleNotFoundError: No module named 'train'` because the wrapper imports a top-level module that is not importable from the installed package. Step 12a / Step 12 now clone the Side-Step source tree at runtime and invoke `python train.py preprocess ...` / `python train.py train ...` directly, with the source checkout added to `PYTHONPATH` for the subprocess.
- 2026-07-02: Confirmed and made explicit that ACE Step 12 full Hybrid training uses `gpu-fulltraining-h100`, not `gpu-smoke-h100`, to stay aligned with the other full model runs. The static Step 12 Azure YAML already targeted `azureml:gpu-fulltraining-h100`; the backend `/api/training/ace/full` route now also tags materialized submissions with `cara_trainer_compute=gpu-fulltraining-h100`. Step 12a Side-Step input preparation remains on the smoke H100 compute as a preparation/validation stage.
