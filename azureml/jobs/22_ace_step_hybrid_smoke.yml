$schema: https://azuremlschemas.azureedge.net/latest/commandJob.schema.json
type: command
command: >-
  python ace_step_hybrid_stages.py
  --stage adapter_smoke
  --ace_tensor_dir ${{inputs.ace_tensor_dir}}
  --output_dir ${{outputs.output_dir}}
  --tensor_manifest_relative_path ${{inputs.tensor_manifest_relative_path}}
  --checkpoint ${{inputs.checkpoint}}
  --planner_checkpoint ${{inputs.planner_checkpoint}}
  --dit_variant ${{inputs.dit_variant}}
  --variant ${{inputs.variant}}
  --max_train_rows ${{inputs.max_train_rows}}
  --max_eval_rows ${{inputs.max_eval_rows}}
  --max_steps ${{inputs.max_steps}}
  --batch_size ${{inputs.batch_size}}
  --learning_rate ${{inputs.learning_rate}}
  --dashboard_triggered ${{inputs.dashboard_triggered}}
  --dry_run ${{inputs.dry_run}}
code: ../../src
inputs:
  ace_tensor_dir:
    type: uri_folder
    mode: ro_mount
    path: azureml://datastores/ds_cara_raw_audio/paths/training-runs/cara-strong-v0.4/ace_step/tensors/
  tensor_manifest_relative_path: ace_tensor_manifest.jsonl
  checkpoint: ACE-Step/Ace-Step1.5
  planner_checkpoint: ACE-Step/acestep-5Hz-lm-0.6B
  dit_variant: base_or_sft_dit
  variant: baseline_lora
  max_train_rows: 4096
  max_eval_rows: 1024
  max_steps: 250
  batch_size: 64
  learning_rate: 0.001
  dashboard_triggered: "false"
  dry_run: "false"
outputs:
  output_dir:
    type: uri_folder
    mode: rw_mount
    path: azureml://datastores/ds_cara_raw_audio/paths/training-runs/cara-strong-v0.4/ace_step/smoke/
environment: azureml:env-ace-step:4
compute: azureml:gpu-smoke-h100
identity:
  type: user_identity
experiment_name: cara-ace-step-smoke
display_name: 22-ace-step-hybrid-smoke
description: Run one ACE-Step Hybrid smoke variant over the CARA-labelled tensor manifest.
tags:
  cara_phase: fine-tuning
  cara_step_id: "06"
  cara_step_name: 06_ace_step_hybrid_smoke
  cara_compute_class: gpu
  cara_model_family: ace_step
  cara_planner_size: 0.6B
  cara_training_gate: ace_step_hybrid_smoke
  cara_training_scope: smoke
  cara_cost_guardrail: existing_azureml_workspace_only
  cara_output_path: azureml://datastores/ds_cara_raw_audio/paths/training-runs/cara-strong-v0.4/ace_step/smoke/
