$schema: https://azuremlschemas.azureedge.net/latest/commandJob.schema.json
type: command
command: >-
  python ace_step_hybrid_stages.py
  --stage planner_probe
  --ace_tensor_dir ${{inputs.ace_tensor_dir}}
  --output_dir ${{outputs.output_dir}}
  --tensor_manifest_relative_path ${{inputs.tensor_manifest_relative_path}}
  --checkpoint ${{inputs.checkpoint}}
  --planner_checkpoint ${{inputs.planner_checkpoint}}
  --dit_variant ${{inputs.dit_variant}}
  --max_rows ${{inputs.max_rows}}
  --dashboard_triggered ${{inputs.dashboard_triggered}}
  --dry_run ${{inputs.dry_run}}
code: ../../src
inputs:
  ace_tensor_dir:
    type: uri_folder
    mode: ro_mount
    path: azureml://datastores/ds_cara_raw_audio/paths/training-runs/cara-strong-v0.4/ace_step/tensors/
  tensor_manifest_relative_path: ace_tensor_manifest.jsonl
  checkpoint: ACE-Step/Ace-Step1.5
  planner_checkpoint: ACE-Step/acestep-5Hz-lm-0.6B
  dit_variant: base_or_sft_dit
  max_rows: 0
  dashboard_triggered: "false"
  dry_run: "false"
outputs:
  output_dir:
    type: uri_folder
    mode: rw_mount
    path: azureml://datastores/ds_cara_raw_audio/paths/training-runs/cara-strong-v0.4/ace_step/planner_probe/
environment: azureml:env-ace-step:4
compute: azureml:cpu-prep-cluster
identity:
  type: user_identity
experiment_name: cara-ace-step-smoke
display_name: 20-ace-step-planner-survival-probe
description: Probe whether CARA pool/family fields survive the ACE-Step planner contract before any DiT trainer claim.
tags:
  cara_phase: fine-tuning
  cara_step_id: "04"
  cara_step_name: 04_ace_step_planner_survival_probe
  cara_compute_class: cpu_capable
  cara_model_family: ace_step
  cara_planner_size: 0.6B
  cara_training_gate: ace_step_planner_probe
  cara_training_scope: planner-probe
  cara_cost_guardrail: existing_azureml_workspace_only
  cara_output_path: azureml://datastores/ds_cara_raw_audio/paths/training-runs/cara-strong-v0.4/ace_step/planner_probe/
